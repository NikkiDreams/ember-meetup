import { validEmail } from 'ember-meetup/helpers/valid-email';
import { module, test } from 'qunit';

module('Unit | Helper | valid email');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = validEmail([42]);
  assert.ok(result);
});
